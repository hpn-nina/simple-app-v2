import React from 'react';

import Footer from '.';

export default {
  title: 'components/Footer',
  component: Footer,
};

const Template = (args) => <Footer {...args} />;

export const Footer_SHOW = Template.bind({});




