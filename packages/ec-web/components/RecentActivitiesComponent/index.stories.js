import React from 'react';

import { RecentActivites_CPN } from '.';

export default {
  title: 'components/RecenctActivitiesComponent',
  component: RecentActivites_CPN,
};

const Template = (args) => <RecentActivites_CPN {...args} />;

export const ExRecentActivities = Template.bind({});
