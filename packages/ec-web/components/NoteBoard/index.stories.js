import React from 'react';

import NoteBoard from '.';

export default {
  title: 'components/NoteBoard',
  component: NoteBoard,
};

const Template = (args) => <NoteBoard {...args} />;

export const NoteBoard_SHOW = Template.bind({});




