import React from 'react';

import FindForm from '.';

export default {
  title: 'components/FindForm',
  component: FindForm,
};

const Template = (args) => <FindForm {...args} />;

export const FindFormDemo = Template.bind({});