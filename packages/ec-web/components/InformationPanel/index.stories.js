import React from 'react';

import { InformationPanel } from '.';

export default {
  title: 'components/InformationPanel',
  component: InformationPanel,
};

const Template = (args) => <InformationPanel {...args} />;

export const ExInformationPanel = Template.bind({});
