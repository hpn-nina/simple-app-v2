import React from 'react';

import StatisticBoard from '.';

export default {
  title: 'components/StatisticBoard',
  component: StatisticBoard,
};

const Template = (args) => <StatisticBoard {...args} />;

export const ExStatisticBoard = Template.bind({});