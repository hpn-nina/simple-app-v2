import React from 'react';

import Category from '.';


export default {
  title: 'components/Category',
  component: Category,
};

const Template = (args) => <Category {...args} />;

export const TestCategory = Template.bind({});